GENERAL | SOFTWARE | INFOSEC | FINANCE

- on hover they change to orange, a hint appears ("manage sources")
- on click a float with source selection opens up
- in the source selection there is one more selector "all", which
marks all sources for that theme at once
- selecting another theme shows the list for that theme only
- change SHOW 20 ITEMS to a dropdown "SHOW ARTICLES" with options 20 (default), 50, 100
- date selector should also open as a float on a click "DATE"

- displaying articles:
    - THEME, SOURCE
    - PUBLISHED AT (format: yyyy-MM-dd, 15min ago) (or 5h ago etc)
    - TITLE (with link) - hover changes color, mouse pointer
    - DESCRIPTION (with link as well) - hover only changes mouse pointer, but not color

